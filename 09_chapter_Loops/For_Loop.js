//for loop

//how to repeat a block of code


for (let i = 0; i < 10; i++) {
    console.log(i);

}


// example of a for loop that counts from 0 to 9. The loop initializes a variable `i` to 0, checks if `i` is less than 10, and increments `i` by 1 after each iteration. Inside the loop, it logs the current value of `i` to the console.

let i = 0;
for (; i < 10; i++) {
    console.log(i);
}